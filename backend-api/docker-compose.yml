services:
  api:
    build: .
    image: verificador-backend
    container_name: verificador-backend 
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./static/banners:/code/static/banners
    restart: always
